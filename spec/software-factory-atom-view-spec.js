'use babel';

import SoftwareFactoryAtomView from '../lib/software-factory-atom-view';

describe('SoftwareFactoryAtomView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
